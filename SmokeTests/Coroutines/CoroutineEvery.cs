namespace LunyScript.SmokeTests.Coroutines
{
	public class CoroutineEvery : Script
	{
		public override void Build(ScriptContext context)
		{
			/*var beat0 = Every(2).Heartbeats().Do(Debug.Log("even beat"));
			var beat1 = Every(2).Heartbeats().Offset(1).Do(Debug.Log("odd beat"));
			var frame0 = Every(2).Frames().Do(Debug.Log("even frame"));
			var frame1 = Every(2).Frames().Offset(1).Do(Debug.Log("odd frame"));

			Coroutine("stop beats").In(10).Frames().Do(beat0.Stop(), beat1.Pause(), Debug.Log("Stopped beat counters"));
			Coroutine("stop frames").In(10).Frames().Do(frame0.Stop(), frame1.Pause(), Debug.Log("Stopped frame counters"));*/
		}
	}
}
